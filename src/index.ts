// NumberTales Misskey AI Bot — エントリポイント
// Phase 1 で本実装を行う。現時点はスケルトン。

async function main(): Promise<void> {
  console.log('NumberTales Misskey AI Bot — starting...');
}

main().catch((err) => {
  console.error('Fatal error:', err);
  process.exit(1);
});
