# NumberTales-MisskeyAIBot

ナンバーテールズのキャラクターとして Misskey 上で応答する Bot の用語集。
共通仕様の正典は [AGENTS.md](./AGENTS.md)。ここには**用語の定義だけ**を置く。

## 数式計算

**数式計算**:
ユーザーが渡した式を、**抽出**・**評価**・**清書**の 3 段で答えとして返す機能の全体。
_Avoid_: 計算（単独で使うと、どの段の話か分からなくなる）

**抽出**:
自然文やスラッシュコマンドから、式の文字列だけを切り出すこと。
これまで「計算できない」と呼ばれてきた失敗の多くは、評価ではなく抽出の失敗だった。
_Avoid_: 解析、パース

**評価**:
抽出した式から値を得ること。評価できる範囲を広げることを「計算能力の強化」と呼ぶ。
_Avoid_: 計算、演算

**清書**:
式や答えを、見やすい形に組み直して表示すること。評価の結果（値）は変えない。
手段は **PM 絵文字**と画像の 2 つで、どちらも PenchantManufacture の字形で組む。
**縦構造**を持つ式と、PM 絵文字では本文に収まらない式は画像にし、それ以外は PM 絵文字で組む。
どちらも使えないときは**プレーン式**だけを載せる。
_Avoid_: 描画、装飾、画像化、デコ

**縦構造**:
1 行の横書きでは表せない、縦方向の組み。分数（`1/3` のような 1 段のものも含む）、入れ子の根号、行列を指す。
_Avoid_: 多段、2 行以上（行数ではなく、分数・根号の入れ子・行列の有無で決まる）

**プレーン式**:
絵文字も画像も使わず、文字だけで書いた式。`×`・`√`・上付きを使った自然な数式で書き、
そのまま `/calc` に貼り直せる。清書した式には必ず併記する（リモートでは PM 絵文字が描画されないため）。
_Avoid_: 生の式、素の式、テキスト式

**厳密値**:
割り算を含み、割り切れない答えを分数で表したもの。小数の答えに**併記**するもので、置き換えはしない。
_Avoid_: 分数モード（モードではなく、常に併記）

**PM 絵文字**:
PenchantManufacture フォントの字形を Misskey のカスタム絵文字にしたもの。清書の手段の 1 つ。
_Avoid_: デコ文字、数字絵文字（Secvier の数字絵文字とは別物）
