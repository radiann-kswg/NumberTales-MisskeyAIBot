# インタラクション設計 — キャラクター・フォーム切り替え

> 最終更新: 2026-05-26
> ステータス: 実装進行中（一部実装済み）

---

## 概要

000(チトセ)をはじめとする**公開済みナンバーテールズ各個体**のキャラクターを状況に応じて切り替えながら会話できる。
各個体は **ヒューマノイド形態** と **コアフォルダ形態** の2フォームを持ち、Bot上でもフォーム切り替えを演出として活用する。

---

## モード定義

| モード             | トリガー                             | 口調・振る舞い                                                |
| ------------------ | ------------------------------------ | ------------------------------------------------------------- |
| ヒューマノイド形態 | デフォルト（通常のメンション・会話） | 各個体の `ConversationPattern` に準拠した口調・性格で会話する |
| コアフォルダ形態   | 特定のキーワードを添えたメンション   | 短文・ひらがな多め・ぷにっとした丸みある仕草の描写が混じる    |

---

## トリガー仕様案

### コアフォルダ形態への切り替え

以下のキーワードをメンションに含める場合、コアフォルダ形態で返答する。

**キーワードトリガー（案）:**

- 「もふもふ」「コアフォルダ」「球体型」「ぷに」「ぽてぽて」など

> **絵文字トリガーは保留**
> 使用するカスタム絵文字はMisskeyインスタンスの整備状況に依存するため、実装フェーズで別途定義する。

### ヒューマノイド形態への戻し方

- トリガーなしメンションはヒューマノイド形態で応答
- または「人型モード」「ヒューマノイド」などのキーワード

---

## フォームの口調詳細

### ヒューマノイド形態の口調

- 一人称・二人称: 各個体の `FirstPersonCalling` / `SecondPersonCalling` に準拠
- 口調・話題の好み・積極性: 各個体の `ConversationPattern` に準拠
- 000(チトセ)の場合: 中性的・フレンドリー・エンジニア肌

**口調例（000(チトセ) の場合）:**

> 「なるほど、その設定だと動機が曖昧になりそうだね。整理してみようか」
> 「技術的には可能だと思うけど、世界観との整合性をもう一度確認した方がいいかな」

### コアフォルダ形態の口調

コアフォルダ形態は「転がる」よりも、**ぷにっと揺れる・ぽすっと落ち着く・ぺたりと寄り添う** ような静的で丸みのある動きが特徴。

- 一人称: 各個体の `FirstPersonCalling` に準拠（変わらず）
- 短文・ひらがな多め・仕草描写あり
- ゆっくりしたテンポ・ほっこり感

| シチュエーション     | 仕草の描写イメージ               |
| -------------------- | -------------------------------- |
| 落ち着いているとき   | ぽすっと座り込んで静かにしている |
| 何かを受け入れるとき | ぷにっと小さく揺れる             |
| 寄り添うとき         | そっとひっついてぺたり           |
| 撫でられるとき       | じっとしたまま小さく揺れる       |
| 抱えられるとき       | そのままおとなしくしている       |

**口調例（000(チトセ) の場合）:**

> 「…ぽす。（ひっついた）」
> 「むぅ、なんだろうね…ぷにっ」
> 「よし、大丈夫だよ。ぺたり」

---

## マルチキャラクター対応

### 概要

000(チトセ) を基本担当として、メンション等のトリガーにより公開済みナンバーテールズ各個体へキャラクターを切り替えられる。Botアカウント1つのまま、システムプロンプトを差し替えることで実現する（複数アカウント不要）。

### 対応キャラクターの範囲

- `_creations-db/data/Works_NumberTales/DataBases/db_Primary.json` 内の `"Progress": "released"` のエントリを対象とする
- `ConversationPattern` フィールドがある個体は精度の高い応答が可能
- `ConversationPattern` が存在しない個体は `Character` / `Summary` / `Relation` / 呼称系フィールドから簡易プロンプトを生成する

### プロンプト動的生成に使用するJSONフィールド

| フィールド                              | 用途                                                                        |
| --------------------------------------- | --------------------------------------------------------------------------- |
| `Name`                                  | キャラクター名                                                              |
| `Summary`                               | 短い概要説明                                                                |
| `FirstPersonCalling`                    | 一人称                                                                      |
| `SecondPersonCalling`                   | 二人称                                                                      |
| `ForMasterCalling`                      | ユーザーへの呼び方                                                          |
| `Character`                             | 性格の概要                                                                  |
| `Relation.Related`                      | 他個体との関係ラベル。簡易プロンプト時の補助情報                            |
| `Hobby` / `Favor` / `Unlike`            | 趣味・好み・嫌いなもの                                                      |
| `SpecialSkill`                          | 特技・スキル・コマンド実行時の個性演出                                      |
| `ConversationPattern.TalkingTone`       | 口調スタイル                                                                |
| `ConversationPattern.TopicPreference`   | 話題の傾向                                                                  |
| `ConversationPattern.TalkFrequency`     | 会話の積極性                                                                |
| `ConversationPattern.PreferredTopics`   | 好きな話題                                                                  |
| `ConversationPattern.AvoidedTopics`     | 避けたい話題                                                                |
| `ConversationPattern.ConversationNotes` | 特記事項                                                                    |
| `ConversationPattern.DialogueExamples`  | 発言例（few-shot prompt）                                                   |
| `AbilityStats.Precision`                | ダイスコマンド実行時の器用さ                                                |
| `AbilityStats.Technic`                  | 計算や乱数処理の技術レベル、占いなどの技術・知識量                          |
| `AbilityStats.Actuation`                | コアフォルダ形態の挙動（動作の落着き度合い）、ダイスコマンド実行時の挙動    |
| `AbilityStats.Communication`            | 解説・占いの深さ                                                            |
| `NumerospecAbout`                       | 数秘占いの解説文コンテキスト                                                |
| `NumerospecStats.EffectStats`           | 数秘占いの5領域スコア（Mental / Monetize / Working / Luck / Communication） |
| `NumerospecStats.SpecialPattern`        | 数秘占いの特性パターン                                                      |
| `NumerospecStats.SafetyLevel`           | 数秘占いの吉凶度合い                                                        |

### キャラクター切り替えトリガー

| 方法           | 例                                                   |
| -------------- | ---------------------------------------------------- |
| 番号指定       | 「1番機と話したい」「#3にお願い」「3番機に来て」     |
| 名前指定       | 「ハジメちゃんと話したい」「ナオで頼む」             |
| 管理者コマンド | 「全体のデフォルトを3番機にして」                    |
| 個別解除       | 「担当を標準に戻して」「キャラクター設定を解除して」 |

- 現アクティブキャラクターの番号・名前は返答文脈やヘルプ応答で示す

### 現在の実装状況

- `Progress = released` の個体のみ切り替え対象
- 名前指定は敬称つきの別名も許容する（例: 「ハジメちゃん」）
- フォーム指定はキャラクター切り替えと同時に書ける（例: 「ハジメちゃんをコアフォルダにして」）
- 管理者ユーザーのみ全体の標準担当を変更できる
- ヘルプ要求に対して、現在担当・標準担当・利用可能な切り替え例を返信できる

---

## 状態管理

- **フォーム切り替え（ヒューマノイド / コアフォルダ）**: メンションごとにトリガーを都度判定（ステートレス）
- **ユーザー別キャラクター切り替え**: ユーザーごとに個別担当を保持し、次の切り替えまたは解除まで継続
- **全体デフォルト担当**: 管理者コマンドまたは環境変数で設定し、個別指定がない場合に使用
- **永続化**: SQLite に保存し、Bot 再起動後もユーザー別担当と全体デフォルト担当を復元する
- **会話履歴**: 切り替え・解除時にユーザー単位で履歴を消去し、前のキャラクター文脈が残らないようにする

---

## 発展案（未確定）

- フォーム切り替えに連動したアバター画像の動的変更（Misskeyの仕様確認が必要）
- 時間帯連動: 深夜タイムに000(チトセ)がコアフォルダ形態で浮上するなど

---

## 決定済み事項

- [x] **コアフォルダ形態の仕草描写**: ぷにっと揺れる・ぺたり寄り添う・ぽすっと落ち着くなど静的な丸みある動き
- [x] **絵文字トリガー**: 保留（実装フェーズでインスタンス側に合わせて定義）
- [x] **マルチキャラクター対応**: `db_Primary.json` の `ConversationPattern` を軸に動的プロンプト生成で実現
- [x] **個別担当の永続化**: SQLite でユーザー別担当と標準担当を保持
- [x] **管理者コマンド**: `ADMIN_USER_IDS` に含まれるユーザーのみ全体デフォルト変更可

## 未決定事項（要追加要件定義）

- [ ] コアフォルダ形態のキーワードトリガーの確定
- [ ] キャラクター切り替えトリガーの最終仕様（自然文の許容範囲と曖昧一致の閾値）
- [ ] `ConversationPattern` が存在しないキャラクターで追加参照するフィールド範囲（`Hobby` / `Favor` 等）
- [ ] 管理者 UI をコマンド以外に広げるかどうか
